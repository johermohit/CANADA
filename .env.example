# Supabase (Public - exposed to client)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Optional compatibility aliases (if your Vercel project already uses these names)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# API Base URL (adjust for dev/prod)
VITE_API_BASE_URL=http://localhost:3000

# Vercel/Server-side only (NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
OPENAI_API_KEY=sk-...
CKAN_API_URL=https://open.canada.ca/data/api/3

# Supabase now uses signing keys internally; this app does not need a legacy JWT secret.
# If another server process needs JWT verification, use the current Supabase signing-key flow.
APP_ORIGIN=http://localhost:5173

# Node env
NODE_ENV=development
